import SwiftUI

struct Theme {
    static let beige = Color(red: 0.96, green: 0.94, blue: 0.89)
    static let text = Color.black
    static let button = Color.blue
    static let buttonText = Color.white
    static let background = Color(red: 0.98, green: 0.97, blue: 0.95)
}
